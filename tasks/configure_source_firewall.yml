---
# Optional firewalld source CIDR restriction (defense in depth with authorized_keys from=).
- name: Удаление общего правила порта при CIDR-restriction
  ansible.posix.firewalld:
    port: "{{ pam_ssh_port }}/tcp"
    permanent: true
    state: disabled
    immediate: true
  when: pam_allowed_source_cidrs | default([]) | length > 0

- name: Rich rules — pam_ssh_port только из разрешённых CIDR
  ansible.posix.firewalld:
    rich_rule: >-
      rule family="ipv4" source address="{{ item }}" port port="{{ pam_ssh_port }}"
      protocol="tcp" accept
    permanent: true
    state: enabled
    immediate: true
  loop: "{{ pam_allowed_source_cidrs }}"
  loop_control:
    label: "{{ item }}"
  when: pam_allowed_source_cidrs | default([]) | length > 0

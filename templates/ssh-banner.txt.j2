MT Bastion / «МТ Доступ» — SSH PAM (Rocky Linux 9, MFA strict)

  • MFA: SSH key + TOTP (6 digits, 30s window)
  • jump operators: use ProxyJump (-J), not direct shell login
  • Lab help: ./scripts/mt-dostup-doctor.sh <operator>

Unauthorized access is prohibited and audited.

#!/usr/bin/env bash
# SSH PAM — host-side JIT expiry purge (systemd timer).
# Managed by Ansible; override bastion_jit_playbook_command in group_vars.
set -euo pipefail
exec {{ bastion_jit_playbook_command }}

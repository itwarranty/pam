---
# QA SSH User CA inventory — copy to inventory/qa.yml

all:
  children:
    bastion_servers:
      children:
        qa:
          hosts:
            bastion-qa.example.com:
              ansible_host: 10.0.0.50
              ansible_user: ansible

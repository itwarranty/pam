---
# Optional FIDO lab operator — merge when pam_fido_lab_enabled: true (PAM_FIDO_LAB=1)
pam_operators_fido_lab:
  - name: engineer-fido
    email: engineer-fido@example.com
    pubkey: "{{ lookup('file', lab_keys_dir + '/engineer-fido.lab.pub') }}"
    mfa_secret: FIDOLABFIDOLABFIDOLABFIDOLABFIDO
    access: jump
    permit_open:
      - "10.0.1.10:22"
    allowed_sources: "{{ pam_lab_allowed_sources | default(['127.0.0.0/8']) }}"

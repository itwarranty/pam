# Локальное тестирование — см. scripts/dev-up.sh (одна команда)

Prod-parity VM + операторы из `group_vars/dev.yml` (lab keys `@mtglobal.team`, TOTP без Vault).

```bash
./scripts/dev-up.sh
```

Ручной путь: `tests/README.md`
